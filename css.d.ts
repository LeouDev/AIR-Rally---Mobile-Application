// Placeholder until `expo start` generates expo-env.d.ts (which brings the
// same wildcard declaration via expo/types) — keeps bare `tsc --noEmit`
// green on a fresh clone.
declare module '*.css';
